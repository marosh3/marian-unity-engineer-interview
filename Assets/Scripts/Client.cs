namespace Superscale {
    public class Client {
        private SuperDuperRememberer rememberer;
        public Client(SuperDuperRememberer rememberer)
        {
            this.rememberer = rememberer;
        }
    }

    // TODO we need a remember functionality taking an array of strings and remembering them as fast as possible
}